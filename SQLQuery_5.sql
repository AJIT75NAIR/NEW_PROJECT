SELECT *
FROM customers
/* RETRIEVE EACH CUSTOMER'S NAME, COUNTRY AND SCORE
*/
SELECT
    first_name,
    country,
    score 
FROM customers
SELECT 
    first_name,
    score,
    country
FROM customers 
SELECT 
    first_name,
    score,
    country
FROM customers 